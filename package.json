{
  "name": "auth",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter @auth/api --filter @auth/web dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
